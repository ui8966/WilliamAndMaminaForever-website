{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "installCommand": "yarn install",
        "buildCommand": "yarn build"
      }
    }
  ]
}
